{
  "name": "dumpster-empire",
  "private": true,
  "version": "0.0.0",
  "description": "Dumpster Empire — monorepo (engine + juego + escritorio Steam)",
  "//versionesPineadas": "Ver DESARROLLO.md §3. No subir sin actualizar ese doc: electron ^31.x, steamworks.js ^0.4.x, electron-builder ^24.x, vitest ^2.x (usado ya en este workspace). Electron/steamworks.js los fija el Agente 6 en apps/desktop.",
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "dev": "npx serve . -l 5173",
    "desktop": "echo 'Agente 6: empaquetado Steam pendiente (apps/desktop)' && exit 1",
    "build": "echo 'Agente 6: build de distribución pendiente (electron-builder)' && exit 1"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
